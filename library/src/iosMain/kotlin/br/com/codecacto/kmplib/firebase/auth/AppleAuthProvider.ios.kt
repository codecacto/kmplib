// Arquivo esvaziado na 2.98.0 — o `actual` mudou para
// iosMain/.../auth/social/AppleAuthProvider.ios.kt (com a sobrecarga que aceita o nonce do
// servidor). O nome antigo sobrevive como `typealias @Deprecated` em
// commonMain/.../firebase/auth/AppleAuthProvider.kt.
// Pode ser removido do disco (o agente não roda `rm`; ver o handoff).
package br.com.codecacto.kmplib.firebase.auth
